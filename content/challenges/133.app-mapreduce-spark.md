---
title: "Ngày 133 — App: wordcount MapReduce và Spark job"
description: "Xây app đếm từ phân tán: MapReduce thủ công đa tiến trình, Spark job tương đương, so sánh tốc độ, shuffle và fault tolerance."
lastUpdate: 2026-09-19
day: 133
difficulty: kho
category: algorithm
estimatedMinutes: 120
tags: [capstone, mapreduce, spark, distributed, wordcount]
prerequisites: [chieu-du-lieu-va-tinh-toan, ram-model-va-chi-phi]
---

# 📔 Day 133: Ngày 133 — App: wordcount MapReduce và Spark job

## Giới thiệu
Bài này xây app đếm từ kiểu phân tán. Chia nhỏ văn bản cho nhiều worker cùng đếm.

## Kiến thức cần nhớ

### MapReduce thủ công 3 bước
- Map: mỗi worker đếm từ cục bộ trong chunk của mình.
- Shuffle: gom cùng 1 từ về cùng 1 reducer theo hash.
- Reduce: cộng tổng từng từ rồi ghi kết quả.
- Ví dụ đời thường: chia xấp phiếu cho nhiều người đếm rồi cộng lại.

### Spark job tương đương
- TextFile chia văn bản thành nhiều partition.
- FlatMap tách từ, map thành cặp từ-1, reduceByKey cộng.
- Giữ dữ liệu trung gian trong RAM nên nhanh hơn ghi đĩa.
- Lineage cho chịu lỗi: mất partition nào tính lại partition đó.

### Combiner và skew
- Combiner cộng cục bộ trước để giảm hàng gửi qua shuffle.
- Skew là vài từ hot làm 1 reducer quá tải.
- Xử lý skew bằng combiner mạnh hoặc salting.
- MapReduce đơn giản chịu lỗi tốt, Spark nhanh nhờ RAM.

```python
# Spark wordcount gọn
rdd = sc.textFile("data.txt")  # chia partition
counts = rdd.flatMap(split_words)  # tách từ
  .map(lambda w: (w, 1))  # cặp từ-1
  .reduceByKey(add)  # cộng phân tán
```

## Ví dụ
- Input: 100MB văn bản, chạy 2 engine. Output: top-10 từ khớp 100%, tổng từ bằng nhau.
- Giải thích: 2 engine khác nhau nhưng cùng logic đếm nên kết quả khớp.
- Input: tăng worker từ 2 lên 4. Output: thời gian giảm ít nhất 30%.
- Giải thích: nhiều tay cùng đếm nên xong sớm hơn.

## 💻 Bài tập
1. Cắt 100MB văn bản thành chunk và đếm cục bộ mỗi mapper. Kiểm tra: tổng từ khớp đếm đơn luồng.
2. Hoàn thiện shuffle gom từ và reducer cộng tổng. Kiểm tra: diff với đơn luồng khớp 100%.
3. Viết Spark job RDD và DataFrame chạy local. Kiểm tra: đọc được DAG trên UI.
4. Giết 1 mapper giữa chừng rồi retry. Kiểm tra: job vẫn xong đúng kết quả.
5. Benchmark 3 cỡ dữ liệu nhân 3 mức worker. Kiểm tra: vẽ đường speedup và kết luận.

## ❓ Câu hỏi ôn tập
- 3 bước map, shuffle, reduce mỗi bước làm gì?
- Combiner giúp giảm tải shuffle thế nào?
- Spark nhanh hơn MapReduce đĩa nhờ gì?
- Skew làm lệch tải reducer ra sao?

---

🌕 Bạn đã hoàn thành Day 133. Làm hết bài tập phía trên rồi sang ngày tiếp theo.
