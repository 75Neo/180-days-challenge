---
title: "DBSCAN - density-reachable, lõi, biên, nhiễu"
description: "Gom cụm dựa mật độ DBSCAN: điểm lõi biên nhiễu, trực tiếp và lan truyền mật độ, thuật toán, cài đặt và ưu nhược điểm."
lastUpdate: 2026-09-19
day: 48
difficulty: trung-binh
category: algorithm
estimatedMinutes: 60
tags: [clustering, dbscan, density]
prerequisites: [thiet-ke-va-kiem-chung-thuat-toan]
---

## Giới thiệu

Bài này học DBSCAN gom cụm theo chỗ đông người.
Thuật toán này bắt được cụm méo và tách nhiễu riêng.

## Kiến thức cần nhớ

### Lõi biên nhiễu

- Epsilon là bán kính nhìn quanh, minPts là số bạn tối thiểu.
- Lõi: quanh mình đủ minPts điểm trong epsilon.
- Biên: không phải lõi nhưng nằm gần một lõi.
- Ví dụ đời thường: chợ đông là lõi, người đứng cửa chợ là biên.

### Lan truyền mật độ

- Trực tiếp: q gần p lõi thì q theo p, chiều một hướng.
- Lan truyền: nối nhiều bước trực tiếp thành chuỗi qua lõi.
- Cùng chuỗi lan truyền thì về chung một cụm.
- Ví dụ đời thường: rủ nhau theo dây chuyền bạn của bạn.

### Thuật toán mở rộng

- Duyệt từng điểm chưa thăm, truy vấn quanh epsilon.
- Không đủ bạn thì gắn nhiễu tạm, đủ thì lập cụm mới.
- Lõi thì nạp hết hàng xóm vào hàng đợi mở rộng.
- Ví dụ đời thường: một người hô, cả xóm đông kéo tới.

| Tiêu chí | K-means | DBSCAN |
|---|---|---|
| Số cụm | Phải cho k trước | Tự tìm, không cần k |
| Hình dạng | Tròn đều | Méo lưỡi liềm vẫn bắt |
| Nhiễu | Ép hết vào cụm | Gắn nhãn nhiễu riêng |

## Ví dụ

- Input: điểm đơn độc xa mọi cụm quá epsilon -> Output: gắn nhiễu cuối cùng.
- Câu này cho thấy kẻ lạc loài bị tách riêng.
- Input: minPts=1 -> Output: mọi điểm thành lõi, mỗi chùm liên thông là một cụm.
- Câu này kiểm tra biên suy biến của tham số.

## 💻 Bài tập

Ngôn ngữ: **Python 3** với NumPy. Không dùng `sklearn.cluster.DBSCAN` trừ khi đối chiếu. Nộp vào `day-048/`.

### Bài 1: DBSCAN cơ bản

```python
NOISE = -1
def dbscan(X: np.ndarray, eps: float, min_pts: int) -> np.ndarray:
    """Trả labels: 0, 1, 2... cho cụm, -1 cho nhiễu. min_pts tính cả chính điểm đó."""
def region_query(X, i, eps) -> list[int]: ...
```

| Yêu cầu | Chi tiết |
| --- | --- |
| Điểm lõi | `len(region_query) >= min_pts` |
| Mở rộng cụm | Hàng đợi. Chỉ điểm lõi mới được nạp tiếp hàng xóm |
| Điểm biên | Được gán vào cụm đầu tiên chạm tới nó |
| Nhiễu tạm | Điểm đang là nhiễu mà sau đó chạm lõi thì đổi thành biên |

```text
3 chùm cách xa + 1 điểm lẻ, eps=0.5, min_pts=4 -> 3 cụm, điểm lẻ = -1
min_pts = 1                                      -> không có nhiễu
eps rất nhỏ                                      -> mọi điểm là nhiễu
```

Viết thêm `point_types(X, eps, min_pts)` trả `"core"`, `"border"`, `"noise"` cho từng điểm.

### Bài 2: Tăng tốc truy vấn vùng

`region_query` quét hết là $O(n)$, tổng $O(n^2)$.

1. Chia lưới ô cỡ `eps`. Chỉ xét 9 ô lân cận (2D).
2. Chạy với $n = 2000, 10000, 50000$. In thời gian 2 cách. Kết quả nhãn phải giống nhau.

### Bài 3: Chọn eps và so với K-means (nâng cao)

1. **k-distance plot:** với mỗi điểm tính khoảng cách tới hàng xóm thứ `min_pts`. Sắp xếp giảm dần và vẽ. Chọn `eps` tại "khuỷu".
2. Chạy DBSCAN và K-means (ngày 47) trên dữ liệu trăng lưỡi liềm và dữ liệu 3 cụm có mật độ khác nhau. Vẽ 4 hình.
3. Viết bảng: khi nào DBSCAN thắng, khi nào K-means thắng.

### ✅ Tiêu chí hoàn thành

- [ ] Test mẫu pass. Nhãn khớp `sklearn` (sau khi đổi tên cụm) trên 3 bộ dữ liệu.
- [ ] Bản lưới cho cùng kết quả và nhanh hơn rõ.
- [ ] Có k-distance plot và 4 hình so sánh.
- [ ] Giải thích được vì sao "density-reachable" không đối xứng.

## ❓ Câu hỏi ôn tập

- Điểm lõi, biên, nhiễu định nghĩa sao?
- Lan truyền mật độ khác trực tiếp thế nào?
- Thuật toán mở rộng cụm chạy ra sao?
- DBSCAN thắng K-means ở điểm nào?

---
🌕 Bạn đã hoàn thành ngày 48. Làm hết bài tập phía trên rồi sang ngày tiếp theo.
