---
title: "MapReduce - map, reduce qua ví dụ Donald Duck"
description: "Mô hình MapReduce qua câu chuyện Donald Duck: song song và phân tán, ánh xạ rồi rút gọn, shuffle và những gì ẩn dưới nắp capo."
lastUpdate: 2026-09-19
day: 50
difficulty: trung-binh
category: algorithm
estimatedMinutes: 60
tags: [mapreduce, parallel, distributed]
prerequisites: [chieu-du-lieu-va-tinh-toan]
---

# 📔 Day 50: MapReduce - map, reduce qua ví dụ Donald Duck

## Giới thiệu
Bài này học MapReduce chia việc lớn cho nhiều máy.
Mô hình này giúp đếm triệu phiếu bầu siêu nhanh.

## Kiến thức cần nhớ

### Song song và phân tán
- Song song: nhiều luồng chung một bộ nhớ một máy.
- Phân tán: nhiều máy riêng bộ nhớ, nói nhau qua mạng.
- Phân tán lo thêm máy chết giữa chừng và mạng chậm.
- Ví dụ đời thường: một bếp nhiều tay so với nhiều bếp nhiều quán.

### Chuyện vịt Donald
- Donald một mình đếm triệu phiếu tới già không xong.
- Thuê đàn vịt con mỗi con đếm một thùng riêng.
- Mỗi con báo cáo số phiếu từng ứng viên thùng mình.
- Ví dụ đời thường: chia lớp chấm thi rồi cộng bảng điểm lại.

### Map rồi reduce
- Map: nhận một mảnh, nhả ra cặp khóa-giá trị trung gian.
- Shuffle: gom mọi giá trị cùng khóa về cùng một máy.
- Reduce: gộp danh sách giá trị thành kết quả cuối.
- Ví dụ đời thường: đếm từ thì map nhả (từ,1), reduce cộng lại.

```python
# Đếm từ bằng map-reduce thu nhỏ
def map_dong(dong):
    return [(tu, 1) for tu in dong.split()]  # mỗi từ một phiếu
def reduce_tu(tu, ds):
    return (tu, sum(ds))  # cộng hết phiếu cùng từ
```

## Ví dụ
- Input: 3 dòng văn bản đếm từ -> Output: tổng đếm bằng tổng số từ vào.
- Câu này kiểm tra không mất không thừa một từ.
- Input: đầu vào rỗng -> Output: đầu ra rỗng, không treo.
- Câu này kiểm tra biên rỗng xử lý sạch.

## 💻 Bài tập
1. Cho 3 dòng văn bản, viết tay đầu ra map, nhóm shuffle, reduce.
Kiểm tra: đúng cặp trung gian, đúng nhóm, đúng đếm cuối.
2. Thiết kế map-reduce tính điểm trung bình từng sinh viên từ file điểm.
Kiểm tra: nêu đúng khóa trung gian và hàm gộp.
3. Mô tả khi một máy map chết giữa chừng thì hệ phục hồi sao.
Kiểm tra: nêu đúng chạy lại nhờ dữ liệu gốc phân tán.
4. Nêu một bài toán không ép được vào map-reduce và lý do.
Kiểm tra: chỉ đúng nhu cầu giao tiếp mịn hoặc lặp nhiều vòng.

## ❓ Câu hỏi ôn tập
- Song song khác phân tán ở đâu?
- Chuyện vịt Donald ánh xạ map reduce sao?
- Shuffle gom dữ liệu thế nào?
- Bài nào không hợp với MapReduce?

---
🌕 Bạn đã hoàn thành Day 50. Làm hết bài tập phía trên rồi sang ngày tiếp theo.
